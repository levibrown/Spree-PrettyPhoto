<% if image = @product.images.first %>
   <% rel = "prettyPhoto#{'[pp_gal]' if product.images.size > 1}" %>
   <%= link_to product_image(@product), image.attachment.url(:large), :rel => rel %>
<% else %>
   <%= product_image(@product) %>
<% end %>
